# PROGRESS — Game Log / GAUNTLET

Ветка: `gauntlet/2026-08-09`. Сессия начата 2026-08-09 с чистого состояния
(один index.html, git-репозитория не было — инициализирован, стартовое
состояние закоммичено в main, работа в ветке).

## Этап 0 — Сбруя ✅

**Сделано:**
- npm-проект: `@playwright/test`, `typescript`; Chromium установлен.
- `tests/server.mjs` — статический сервер (APP_DIR переключает корень:
  сейчас репо, после Этапа 1 — dist/).
- `playwright.config.ts` — 4 проекта: mobile-ru/en (390×844, Pixel 7),
  desktop-ru/en (1280×800). Язык через localStorage в init-скрипте.
- `tests/fixtures.ts` — сеяный PRNG (mulberry32), `makeState(1500)` с
  реалистичным распределением (серии, «Давно», перепрохождения, заметки),
  `tinyState()` — ручная фикстура для точечных проверок. Реальные данные
  пользователя не используются нигде.
- Смоуки всех пунктов §9: `smoke-core` (вкладки/статы, годовые группы,
  сворачивание+persist, «+ Прошёл в…», звёзды+cs, заметки, подсказки,
  чип серии, избранное, сортировки, поиск+#nodata, пустые состояния),
  `smoke-actions` (кубик, объединение дублей + исключения сиквелов/серий
  + nomerge-reset, импорт CSV по правилам §3, бэкап/восстановление),
  `smoke-sync` (мок Gist: pull новее, push при локальном новее, деградация
  в офлайн; открытие приложения НЕ бампает updatedAt),
  `smoke-shell` (RU/EN, PWA manifest+иконки, десктопная сетка,
  отсутствие горизонтального переполнения на 390px).
- Автопроверка чистой консоли в каждом тесте (§7).
- `npm run shots` — 6 экранов × 4 проекта в `shots/`.
- **Итог: 118 passed, 10 skipped (форм-специфичные), 0 failed.**

**Найденные и починенные баги текущего кода:**
1. `syncConnect`: `var t` (токен) затеняла функцию перевода `t()` →
   при подключении синка летел `TypeError: t is not a function`, тосты
   «Подключено»/«Вставь токен» не работали. Переименовано в `tok`.
2. Десктопная сетка ≥860px заявлена в §9, но в коде отсутствовала
   (body был зажат в 560px на любом экране). Добавлена media query:
   ≥860px — body 1024px, секции в 2 колонки, заголовок на всю ширину.

**Известные мелочи (не блокеры, отложено):**
- Захардкоженная RU-строка в aria-label звёзд («Оценка N из 5») — вне
  словаря TR. Починить при модуляризации.
- ~~Тап по кнопке-подсказке закрывал открытую карточку~~ — починено
  перед Этапом 4 (сквозной click-обработчик теперь игнорирует .sugbtn),
  смоук подсказок обновлён: карточка остаётся открытой.

**Как запустить:** `npm test` (смоуки), `npm run shots` (скриншоты),
`npm run serve` (локальный сервер на :8123).

## Этап 1 — Модуляризация ✅

**Сделано:**
- Код разнесён по `src/`: `types.ts` (схема §3), `util.ts` (esc/norm/toast),
  `i18n.ts` (TR + t/stLabel/setLang), `data.ts` (state, load/save/migrate,
  onSave-хуки), `sync.ts` (файл-автосейв + Gist/Apps Script + панель),
  `importer.ts` (CSV §3 + бэкап/восстановление), `ui.ts` (render, cardHTML,
  все события, applyLang), `main.ts` (порядок инициализации как в IIFE).
- Vite + vite-plugin-singlefile → **один самодостаточный dist/index.html
  (56 КБ против старых 72 КБ — бюджет ×1.5 соблюдён с запасом)** +
  manifest.json и иконки из `public/`.
- `npm test` теперь: сборка (`pretest`) + полный смоук на dist.
  **130 passed / 0 failed** на сборке.
- Корневые index.html/manifest/иконки удалены — источник истины `src/`
  + `public/` (легаси-файл остался в истории git, коммит 9fbbe7a).
- `.github/workflows/deploy.yml` — сборка и деплой dist/ в Pages
  (создан, не запушен: пользователь включит Pages сам).
- Функциональный дифф старого файла и порта — построчная сверка функций
  субагентом-критиком + идентичный набор смоуков на обоих вариантах.
- Попутно вычищены захардкоженные строки вне словаря (§3): aria-label
  звёзд и шестерёнки, «не тот формат», «Данные журнала», «автосохранение»,
  «Не дубли», « — токен не подошёл» — новые ключи TR в обоих языках.
- Критик сверил порт пофункционно с оригиналом: принято. Единственное
  сознательное отличие сверх списка: после ошибки импорта CSV
  (нет строк / нет колонки Name) файловый инпут теперь очищается, и
  повторный выбор того же файла снова запускает импорт (в оригинале
  инпут «залипал» — повторный выбор молча игнорировался).

## Этап 2 — Экран «Итоги» ✅

**Сделано:**
- Кнопка ▦ в шапке (рядом с ⚙) → полноэкранное FF-окно `src/results.ts`.
- Переключатель периодов: «Всё время» + каждый год с прохождениями +
  «Давно» (чипы, активный золотой).
- Метрики периода: пройдено игр, прохождений (если есть перепрохождения),
  часов в играх, средняя оценка звёздами; «Игра года» (высшая своя оценка,
  при равенстве — больше часов), «Самая длинная», «Серия года»;
  топ-3 жанров и платформ; разбивка по лончерам CSS-барами (градиентное
  золото, без библиотек). Пустое состояние, RU/EN, Esc/✕.
- Смоуки `tests/smoke-results.spec.ts` (7 сценариев × 4 проекта).
- Критик: 1-й заход ОТКЛОНЕНО (тап-цели чипов/✕ < 44px, разваливающиеся
  широкие строки, обрубки точечных лидеров, бары не показаны на шотах) —
  все 6 правок внесены (блочная раскладка широких строк, min-height 44px,
  span.rnb для неразрывных значений, отдельный скриншот баров) —
  2-й заход **ПРИНЯТО**.

## Этап 3 — Умный кубик ✅

**Сделано:**
- Тап по 🎲 (дайсбар и кнопка в заголовке беклога) открывает малое
  FF-окно `src/dice.ts`: жанр (из имеющихся в беклоге), community
  score ≥ 60/70/80/90, «есть в библиотеке» (source не пуст), «только
  нетронутые» (time == 0), серия; живой счётчик пула.
- Фильтры запоминаются на устройстве (`gamelog-dice-v1` в localStorage,
  в синк не попадают).
- «Бросить» крутит только по отфильтрованному пулу; пустой пул — честное
  сообщение в окне (окно не закрывается); «Без фильтров» — быстрый
  бросок по всему беклогу; «Сбросить фильтры» — ghost-кнопка.
- Бросок вынесен в `ui.rollDice(pool)`; обработчик тапа по 🎲 подменяется
  через `setDiceHandler` (без циклических импортов).
- Смоуки `tests/smoke-dice.spec.ts`: фильтр по жанру/оценке/серии,
  пустой пул, сброс, персистентность после перезагрузки, быстрый бросок,
  Esc. Старые смоуки кубика обновлены под новый сценарий.
- Критик (мобайл): 1-й заход НЕ ПРИНЯТО (6 правок: «Любая» для серии,
  русская плюрализация «151 игра», кастомные FF-чекбоксы вместо нативных,
  тап-цели 44px, центрирование, вес кнопки сброса) — всё внесено,
  повторный заход — **ПРИНЯТО**.

## Этап 5 — Офлайн и производительность ✅ (тесты зелёные)

**Сделано:**
- `public/sw.js` — service worker app-shell: network-first с фолбэком в
  кэш `gamelog-shell-v1` (только same-origin GET; api.github.com и
  script.google.com не трогает — синк сам деградирует в «офлайн ·
  локально»). Регистрация в `main.ts`.
- Playwright-тест офлайна (`tests/smoke-offline.spec.ts`): первый визит
  кэширует шелл → `context.setOffline(true)` → reload → приложение
  открывается, данные из localStorage на месте. В остальных тестах SW
  блокируется конфигом (`serviceWorkers: "block"`), иначе он обходил бы
  сетевые моки.
- Перф-пробы `__firstRenderMs`/`__lastRenderMs` в main/render (§7).
- **Замеры на фикстуре 1500 игр (сборка dist):**
  - первый рендер: **14.5 мс** десктоп / **15.3 мс** мобайл (бюджет < 150);
  - перерендер при вводе в поиск: **3.5 мс** (кадры не блокируются);
  - бюджеты §7 зашиты в тесты — регресс уронит CI.

## Следующий шаг
Этап 4: цикл полировки всех экранов под критиком — идёт.
